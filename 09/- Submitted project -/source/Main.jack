/** Initializes a new canvas and starts running it. */
class Main {
    function void main() {
        var Canvas canvas;
        let canvas = Canvas.new();
        do canvas.run();
        do canvas.dispose();
        return;
    }
}
